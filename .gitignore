# Virtual environment
venv/
__pycache__/
*.pyc

# Secrets — never commit real tokens/keys
קודים לTELEGRAM.txt
.env
# config.py holds real profile info alongside the standard settings —
# kept local-only per earlier request; config.example.py is the committed template.
config.py

# Real people's data — never commit
known_connections.json
Connections.csv

# NOTE: seen_jobs.json / api_usage.json / outreach_log.jsonl are NOT
# git-ignored — they live under state/ and ARE tracked on purpose: the
# daily GitHub Actions run commits updates there so dedup/budget state
# survives across runs even though each run starts from a fresh checkout.

logs/

# Scratch / working files (e.g. artifact report sources) — not part of the repo
.scratch/
